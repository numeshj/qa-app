{
  "name": "qa-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "postinstall": "pnpm -r prepare",
    "build": "pnpm -r build",
    "dev": "pnpm --recursive --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  }
}
